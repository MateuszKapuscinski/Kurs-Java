package pl.sda.zdjavapol75.zaawansowana.kolekcje.mapy.zadSlajd64;

public class SkorowidzLiter {

/*   public Map<String, Set<Integer>> skorowidzLiterowy (String tekst) {
        String[] strings = tekst.split("[^a-z^A-Z]");
        Map<String, Set<Integer>> wystapienieSlowa= new HashMap<>();
        for (String string : strings) {
            if(wystapienieSlowa.containsKey(string)){
                wystapienieSlowa.put(string, wystapienieSlowa.get(string) + 1);
            } else {
                wystapienieSlowa.put(string, 1);
            }
        }
        return
    }*/
}
